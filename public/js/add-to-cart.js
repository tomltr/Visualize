// clickMe(() => {
//     console.log(`You clicked index`);
// });

// add_item((current_user, product) => {
//     console.log(`user: ${current_user}`);
//     console.log(`product: ${product.product_title}`);

// });

function add_to_cart(user, product) {
    if (user) {
        alert(`${product} added to cart`);
    }
};